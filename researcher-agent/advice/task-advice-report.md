# mcp-skills â€” Full Task Advice

> Generated by the local **mcp-skills** MCP server (`get_task_advice`).
> Dashboard: http://localhost:8787  Â·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a researcher AI agent: conduct deep research on any topic using web search, documentation, sources exploration; verify facts and source quality; produce structured, citation-backed research reports; design research workflow using skills, workflows, MCP servers and subagents |
| Requirement | Build a researcher AI agent that conducts deep research on any topic using web search, documentation, and source exploration; verifies facts and source quality; and produces structured, citation-backed research reports, with a workflow designed using skills, workflows, MCP servers, and subagents. |
| Intent | Create an autonomous AI agent for deep research and verified, citation-backed report generation. |
| Advice elapsed | 235.1 s |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows (all 7 catalogs) |

## Summary

Deploy ml-explore-run and pi-web-search skills for search and source exploration, connect hannesrudolph-mcp-ragdocs for documentation retrieval, coordinate research subagents with crewai, use ripgrep and Playwright to verify sources, and consult anthropic-api-docs and realtime-chat-docs to produce structured, citation-backed reports with product-catalog and search features.

## Recommended (top hits per catalog)

**skills**: ml-explore-run, pi-web-search
**workflow**: fix-slow-checkout-page
**mcpserver**: hannesrudolph-mcp-ragdocs
**agent**: crewai
**docs**: anthropic-api-docs, realtime-chat-docs
**tools**: ripgrep, Playwright
**features**: product-catalog, search

## Plan

### Phase: Setup
- Load skills: ml-explore-run, pi-web-search
- Connect MCP server: hannesrudolph-mcp-ragdocs
- Initialize agent/platform: crewai
### Phase: Implement
- Execute workflow: fix-slow-checkout-page
- Use ripgrep and Playwright for source exploration and verification
- Include features: product-catalog, search
### Phase: Verify
- Consult documentation: anthropic-api-docs, realtime-chat-docs
- Verify citations and source quality, then finalize

## Ordered Steps

1. Use ripgrep and Playwright for source exploration and verification; include product-catalog and search
2. Consult anthropic-api-docs and realtime-chat-docs to validate outputs
3. Implement, verify citations and source quality, then complete
4. Live research first: catalog coverage is weak (skills, workflows, mcpservers). Run discover_all('Build a researcher AI agent that conducts deep research on any topic using web search, documentation') and web-search the official repos, then re-run get_task_advice.
5. PERSIST everything into the working project: save every skill/workflow/MCP/agent/docs you load under .mcp-skills/ (skills/, workflows/, mcpservers/, agents/, docs/) plus this full advice under .mcp-skills/advice/, write/update AGENTS.md at the project root with the manifest + this plan, and set up a knowledge graph in the project: Obsidian vault (.obsidian + linked notes per domain/decision/component) and a Graphify code graph (graphify-out/graph.json built from the codebase).
6. REPORT BACK after each step: update the Obsidian notes + Graphify graph with what changed, run a graph query (graphify query/path/explain) and pass ONLY the relevant graph summary + this advice as context to the LLM (prevents token burning), then call get_task_advice with the updated status, persist the refreshed advice into .mcp-skills/advice/, and continue - never run multiple steps without re-checking.

## Cautions

- Verify facts and source quality before adding any citation to the report.
- Use exact catalog names from the report; do not substitute alternatives.
- Run verification after each phase before moving to the next step.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("ml-explore-run")`
- `get_skill("pi-web-search")`
- `get_skill("deep-research")`
- `get_skill("ml-ai-research-explore")`
- `get_skill("scientific-writing")`
- `get_skill("search-specialist")`
- `get_skill("auto-research")`
- `get_skill("ms-azure-search-documents-dotnet")`

### workflows
- `get_workflow("fix-slow-checkout-page")` â†’ workflows/general/fix-slow-checkout-page.md
- `get_workflow("fw-research-optimize-execute")` â†’ workflows/framework/fw-research-optimize-execute.md
- `get_workflow("performance-optimization")` â†’ workflows/performance/performance-optimization.md
- `get_workflow("fw-model-evaluation")` â†’ workflows/framework/fw-model-evaluation.md
- `get_workflow("nextjs-app-build")` â†’ workflows/frontend/nextjs-app-build.md

### mcpservers
- `get_mcp_server("hannesrudolph-mcp-ragdocs")`
- `get_mcp_server("exa-mcp")`
- `get_mcp_server("rag-documentation-mcp-server")`
- `get_mcp_server("rag-documentation")`
- `get_mcp_server("mcp-documentation-server")`

### agents
- `get_agent("crewai")`
- `get_agent("trae")`
- `get_agent("openhands")`

### docs
- `https://docs.anthropic.com`
- `https://socket.io/docs`
- `https://gofastmcp.com`

### tools
- `https://github.com/BurntSushi/ripgrep`
- `https://playwright.dev`
- `https://www.selenium.dev`

### features
- ``
- `https://apis.guru`
- ``

## Combined ranking functions (best default per catalog)

- skills : `get_skills_combined(task)`
- workflows : `get_workflows_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- tools : `get_tools_combined(task)`
- features : `get_features_combined(task)`

## Coverage (per catalog hit strength)

- skills : 0.033
- workflows : 0.033
- mcpservers : 0.033
- agents : 0.105
- docs : 0.36
- tools : 0.176

## Research note

- tool: `discover_all("Build a researcher AI agent that conducts deep research on any topic using web search, documentation, and source explora")`
- weak catalogs: skills, workflows, mcpservers
- note: Catalog coverage is weak for this task (skills, workflows, mcpservers) - the ranked hits may be irrelevant. Run live research first, then re-run get_task_advice.

- `search_mcp_servers(query="<key tool>")`
- `search_agents(query="<key tool>")`
- `web search the official repos/docs for exact URLs and install commands`

## Methodology Report (21 sections)

### [executive_summary] Executive Summary

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")` `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")`

Build a researcher agent using crewai orchestration, ml-explore-run and pi-web-search skills, and hannesrudolph-mcp-ragdocs for documentation retrieval. Use ripgrep and Playwright to verify sources and produce structured, citation-backed reports with product-catalog and search features. Validate outputs against anthropic-api-docs and realtime-chat-docs.

### [goals] Goals & Success Criteria

**Functions:** `get_features_combined(task)` `get_features_combined("product-catalog")` `get_features_combined("search")` `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")`

Implement a deep-research agent that searches web, documentation, and source code, verifies facts and source quality, and outputs structured citation-backed reports. Integrate product-catalog and search features. Use exact catalog components: hannesrudolph-mcp-ragdocs, exa-mcp, rag-documentation-mcp-server, crewai, ml-explore-run, pi-web-search, deep-research, ripgrep, Playwright, and Selenium.

### [constraints] Core Engineering Constraints

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")` `get_docs_combined(task)` `get_related_docs("anthropic-api-docs")` `get_related_docs("realtime-chat-docs")`

Use exact catalog names from the report; do not substitute alternatives. Verify facts and source quality before adding any citation. Run verification after each phase before moving to the next step. Use the locally-running agent-mcp-orchestrator to discover MCP servers before wiring them into the workflow.

### [architecture] Target Architecture & Stack

**Functions:** `get_mcpservers_combined(task)` `get_mcp_server("hannesrudolph-mcp-ragdocs")` `get_mcp_server("exa-mcp")` `get_agents_combined(task)` `get_agent("crewai")` `get_agent("trae")` `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Coordinate research subagents with crewai. Use ml-explore-run and pi-web-search for search and source exploration, hannesrudolph-mcp-ragdocs for documentation retrieval, and ripgrep plus Playwright for source verification. Include product-catalog and search features in the report generation pipeline. Consult anthropic-api-docs and realtime-chat-docs to validate report structure.

### [repo] Repository / Monorepo Strategy

**Functions:** `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")` `get_docs_combined(task)` `get_related_docs("anthropic-api-docs")` `get_related_docs("realtime-chat-docs")`

Organize the project as a crewai codebase with skills loaded from ml-explore-run and pi-web-search. Use ripgrep to explore local source and Playwright or Selenium for live source verification. Keep MCP server configurations for hannesrudolph-mcp-ragdocs, exa-mcp, and rag-documentation-mcp-server in the repository.

### [domains] Functional Domain Decomposition

**Functions:** `get_features_combined(task)` `get_features_combined("product-catalog")` `get_features_combined("search")` `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Target web search, documentation retrieval, source exploration, fact verification, and structured reporting. Use product-catalog and search features to organize research outputs. Apply reviews-ratings when assessing source quality and use fix-slow-checkout-page as a performance-optimization test case.

### [data] Data Architecture & Schema

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")` `get_docs_combined(task)` `get_related_docs("anthropic-api-docs")` `get_related_docs("realtime-chat-docs")`

Ingest data from web search via pi-web-search, documentation via hannesrudolph-mcp-ragdocs, and source exploration via ripgrep and Playwright. Maintain citation metadata and source-quality scores. Use product-catalog and search features to index retrieved sources and reports.

### [api] API Methodology

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Use anthropic-api-docs and realtime-chat-docs to shape report generation and citation formatting. Use fastmcp-docs to configure MCP servers. Call agent-mcp-orchestrator methods get_mcp_status, list_mcp_servers, get_server_info, get_server_logs, and get_open_incidents to discover and monitor available APIs.

### [frontend] Frontend / UI Methodology

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Build a product-catalog and search frontend for browsing research reports. Use Playwright and Selenium to automate UI verification. Use trae or openhands as the agent platform for frontend and interactive review tasks.

### [agent_model] Agent-MCP Operating Model

**Functions:** `get_agents_combined(task)` `get_agent("crewai")` `get_agent("trae")` `get_mcpservers_combined(task)` `get_mcp_server("hannesrudolph-mcp-ragdocs")` `get_mcp_server("exa-mcp")`

Create crewai subagents: a research agent using ml-explore-run and pi-web-search, a documentation agent using hannesrudolph-mcp-ragdocs, and a verification agent using ripgrep and Playwright. Coordinate them with the deep-research workflow. Use fw-research-optimize-execute to optimize execution.

### [system_mcp] System MCP Discovery (agent-mcp-orchestrator)

**Functions:** `get_mcp_status()` `list_mcp_servers(filter="")` `get_server_info(name="<server-name>")` `get_server_logs(server="<server-name>", limit=20)` `get_open_incidents()`

Before starting, call agent-mcp-orchestrator's list_mcp_servers to discover every MCP server on the system. Use get_mcp_status and get_server_info to check availability and capabilities, get_server_logs to debug failures, and get_open_incidents to identify degraded servers. Wire suitable discovered servers, especially hannesrudolph-mcp-ragdocs, exa-mcp, and rag-documentation-mcp-server, into the other sections of this report. Re-run get_mcp_status after each phase to confirm connectivity.

### [knowledge] Knowledge / Memory Model

**Functions:** `get_tools_combined(task)` `get_tools_combined("ripgrep")` `get_tools_combined("Playwright")` `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Store retrieved documentation and source excerpts in a knowledge base indexed by product-catalog and search features. Use the deep-research skill to organize findings. Consult anthropic-api-docs and realtime-chat-docs for the report schema and citation format.

### [project_knowledge] Project Knowledge Persistence & Re-Advice Loop

**Functions:** `get_skill(name) -> save to .mcp-skills/skills/<name>/SKILL.md` `get_workflow(name) -> save to .mcp-skills/workflows/<name>.md` `get_mcp_server(name) -> save to .mcp-skills/mcpservers/<name>.json` `get_agent(name) -> save to .mcp-skills/agents/<name>.json` `save advice.report + categories + how_to_read -> .mcp-skills/advice/<date>-<slug>.json` `write/update AGENTS.md (project root) with the manifest + plan` `set up Obsidian vault (.obsidian + linked notes) + Graphify code graph (graphify-out/graph.json)` `after each step: update both graphs, query the graph (graphify query/path/explain)` `pass ONLY the graph summary + this advice as context to the LLM (saves tokens)` `after each step: get_task_advice(updated status) -> apply refreshed advice`

Maintain a project knowledge file listing MCP servers discovered via agent-mcp-orchestrator and their statuses. Record verification results from ripgrep and Playwright. Update project knowledge after each phase with decisions and source-quality assessments.

### [lifecycle] Full Development Lifecycle

**Functions:** `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")`

Follow Setup, Implement, and Verify phases. In Setup, load skills and connect MCP servers. In Implement, execute the fix-slow-checkout-page workflow and use ripgrep and Playwright. In Verify, consult anthropic-api-docs and realtime-chat-docs, verify citations, and finalize the report.

### [testing] Testing Strategy

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")` `get_tools_combined(task)` `get_tools_combined("ripgrep")` `get_tools_combined("Playwright")`

Use Playwright and Selenium to test product-catalog and search features. Use ripgrep to verify citations against local source and documentation. Run verification after each phase before moving to the next step.

### [deployment] Deployment & Environments

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")` `get_mcpservers_combined(task)` `get_mcp_server("hannesrudolph-mcp-ragdocs")` `get_mcp_server("exa-mcp")`

Deploy the crewai agent with hannesrudolph-mcp-ragdocs, exa-mcp, and rag-documentation-mcp-server configured. Ensure agent-mcp-orchestrator reports all MCP servers healthy via get_mcp_status. Use trae or openhands for deployment monitoring.

### [phases] Implementation Phases

**Functions:** `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")`

Setup: load ml-explore-run and pi-web-search, connect hannesrudolph-mcp-ragdocs, and initialize crewai. Implement: execute fix-slow-checkout-page, use ripgrep and Playwright, and include product-catalog and search features. Verify: consult anthropic-api-docs and realtime-chat-docs, verify citations and source quality, then finalize.

### [backlog] Master Backlog / TODOs

**Functions:** `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")`

Add integration tasks for exa-mcp and rag-documentation-mcp-server. Add reviews-ratings source scoring and the fw-research-optimize-execute workflow. Track performance-optimization fixes derived from fix-slow-checkout-page.

### [done] Definition of Done

**Functions:** `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")` `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Done means all phases verified, citations checked against sources, and a structured report generated with product-catalog and search features. All MCP servers discovered via agent-mcp-orchestrator are healthy. No unverified citations remain.

### [risks] Risks & Mitigations

**Functions:** `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

MCP server outages or degraded status may be detected via get_open_incidents. Source quality varies; use reviews-ratings and ripgrep or Playwright verification. Exact catalog names must be used; substitutions break the workflow.

### [checklist] First Execution Checklist

**Functions:** `get_workflows_combined(task)` `get_workflow("fix-slow-checkout-page")` `get_workflow("fw-research-optimize-execute")` `get_skills_combined(task)` `get_skill("ml-explore-run")` `get_skill("pi-web-search")`

Load skills, connect MCP servers, initialize crewai, execute the workflow, use ripgrep and Playwright, include product-catalog and search, consult docs, verify citations, and finalize the report.

