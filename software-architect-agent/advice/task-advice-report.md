# mcp-skills - Full Task Advice

> Generated by the local **mcp-skills** MCP server (`get_task_advice`).
> MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a software architect AI agent: design and review software architectures and system design; define module boundaries, layers and interfaces; produce architecture decision records (ADRs), C4 diagrams and architecture documents; apply domain-driven design, microservices and integration patterns; evaluate trade-offs, scalability, security and technical debt; use workflows, skills, MCP servers and subagents |
| Advice elapsed | 283.0 s |
| Categories returned | mcpservers, workflows, skills, tools, features, agents, docs (all 7 catalogs) |

## Summary

Build an AI architect agent on opencode that loads the software-architecture and architect-review skills, follows fw-feature-delivery, and uses the narasimhaponnada-mermaid-mcp server to produce ADRs and C4 diagrams. Apply DDD, microservices, and integration patterns; validate with Terraform and Mermaid Live, consult deepseek-api-docs and flask-docs, and include it-asset-management and service-desk features.

## Recommended (top hits per catalog)

- **skills**: software-architecture, architect-review
- **workflow**: fw-feature-delivery
- **mcpserver**: narasimhaponnada-mermaid-mcp
- **agent**: opencode
- **docs**: deepseek-api-docs, flask-docs
- **tools**: Terraform, Mermaid Live
- **features**: it-asset-management, service-desk

## Plan

### Phase: Initialize
- Load skills: software-architecture, architect-review
- Set workflow: fw-feature-delivery on opencode
- Connect MCP server: narasimhaponnada-mermaid-mcp

### Phase: Design
- Apply DDD and microservices/integration patterns to define module boundaries, layers, and interfaces
- Produce ADRs and C4 diagrams with Mermaid via the MCP server
- Draft architecture documents and review with architect-review

### Phase: Integrate & Verify
- Consult deepseek-api-docs and flask-docs for implementation constraints
- Use Terraform for infrastructure-as-code validation and Mermaid Live for diagram checks
- Include it-asset-management and service-desk features
- Verify the completed architecture before finishing

## Ordered Steps

1. Produce ADRs, C4 diagrams, and architecture documents; review with architect-review.
2. Consult deepseek-api-docs and flask-docs; validate with Terraform and Mermaid Live; include it-asset-management and service-desk.
3. Implement and verify before completing.
4. Live research first: catalog coverage is weak (skills, workflows, mcpservers, agents, docs). Run discover_all('Build a software architect AI agent that designs and reviews software architectures, defines module ') and web-search the official repos, then re-run get_task_advice.
5. PERSIST everything into the working project: save every skill/workflow/MCP/agent/docs you load under .mcp-skills/ (skills/, workflows/, mcpservers/, agents/, docs/) plus this full advice under .mcp-skills/advice/, write/update AGENTS.md at the project root with the manifest + this plan, and set up a knowledge graph in the project: Obsidian vault (.obsidian + linked notes per domain/decision/component) and a Graphify code graph (graphify-out/graph.json built from the codebase).
6. REPORT BACK after each step: update the Obsidian notes + Graphify graph with what changed, run a graph query (graphify query/path/explain) and pass ONLY the relevant graph summary + this advice as context to the LLM (prevents token burning), then call get_task_advice with the updated status, persist the refreshed advice into .mcp-skills/advice/, and continue - never run multiple steps without re-checking.

## Cautions

- Do not skip architect-review after producing ADRs and C4 diagrams.
- Keep diagram syntax compatible with narasimhaponnada-mermaid-mcp and Mermaid Live.
- Validate Terraform changes before applying to avoid infrastructure drift.
- Ensure DDD module boundaries align with it-asset-management and service-desk feature requirements.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("software-architecture")`
- `get_skill("architect-review")`
- `get_skill("ontoly-software-graph")`
- `get_skill("event-sourcing-architect")`
- `get_skill("ms-cloud-solution-architect")`
- `get_skill("architecture-patterns")`
- `get_skill("design-orchestration")`
- `get_skill("brave-man")`

### workflows
- `get_workflow("fw-feature-delivery")` -> workflows/framework/fw-feature-delivery.md
- `get_workflow("fw-model-development")` -> workflows/framework/fw-model-development.md
- `get_workflow("fw-code-review")` -> workflows/framework/fw-code-review.md
- `get_workflow("fw-compliance-audit")` -> workflows/framework/fw-compliance-audit.md
- `get_workflow("documentation-generation")` -> workflows/docs/documentation-generation.md

### mcpservers
- `get_mcp_server("narasimhaponnada-mermaid-mcp")`
- `get_mcp_server("erajasekar-ai-diagram-maker-mcp")`
- `get_mcp_server("ng-diagram-mcp-server")`
- `get_mcp_server("uml-mcp")`
- `get_mcp_server("mcp-expert-server")`

### agents
- `get_agent("opencode")`
- `get_agent("crewai")`
- `get_agent("openhands")`

### docs
- `https://api-docs.deepseek.com`
- `https://flask.palletsprojects.com`
- `https://developers.google.com/maps`

### tools
- `https://www.terraform.io`
- `https://mermaid.live`
- `https://www.drawio.com`

### features
- `https://www.isaca.org/resources/cobit`
- `https://www.axelos.com/certifications/itil-service-management`
- `https://www.binance.com/en/research`

## Combined ranking functions (best default per catalog)

- skills : `get_skills_combined(task)`
- workflows : `get_workflows_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- tools : `get_tools_combined(task)`
- features : `get_features_combined(task)`

## Coverage (per catalog hit strength)

- skills : 0.033
- workflows : 0.033
- mcpservers : 0.033
- agents : 0.033
- docs : 0.033
- tools : 0.167

## Research note

- research_needed: True
- tool: `discover_all("Build a software architect AI agent that designs and reviews software architectures, defines module boundaries, layers, ")`
- weak catalogs: skills, workflows, mcpservers, agents, docs

## Methodology Report (21 sections)

### [executive_summary] Executive Summary

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review") Â· get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development")

Build an AI architect agent on opencode that loads software-architecture and architect-review skills, follows fw-feature-delivery, and uses MCP servers to produce ADRs, C4 diagrams, and architecture documents. Apply DDD, microservices, and integration patterns to define module boundaries for it-asset-management and service-desk. Validate designs with Terraform and Mermaid Live, and consult deepseek-api-docs and flask-docs before finalizing.

### [goals] Goals & Success Criteria

**Functions:** get_features_combined(task) Â· get_features_combined("it-asset-management") Â· get_features_combined("service-desk") Â· get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development")

Produce ADRs, C4 diagrams, and architecture documents for it-asset-management and service-desk features. Define module boundaries, layers, and interfaces using DDD, microservices, and integration patterns. Ensure every architecture artifact passes architect-review and is validated against Terraform and Mermaid Live.

### [constraints] Core Engineering Constraints

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review") Â· get_docs_combined(task) Â· get_related_docs("deepseek-api-docs") Â· get_related_docs("flask-docs")

Use opencode as the agent runtime and fw-feature-delivery as the workflow. Keep Mermaid syntax compatible with narasimhaponnada-mermaid-mcp and Mermaid Live. Do not skip architect-review after producing ADRs or C4 diagrams. Validate Terraform changes before applying to avoid infrastructure drift.

### [architecture] Target Architecture & Stack

**Functions:** get_mcpservers_combined(task) Â· get_mcp_server("narasimhaponnada-mermaid-mcp") Â· get_mcp_server("erajasekar-ai-diagram-maker-mcp") Â· get_agents_combined(task) Â· get_agent("opencode") Â· get_agent("crewai") Â· get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Design a modular architecture with DDD-aligned bounded contexts for it-asset-management, service-desk, and defi-integration. Define microservice boundaries, layers, and interfaces using integration patterns such as API gateway, event-driven messaging, and saga. Generate C4 context, container, component, and code diagrams with narasimhaponnada-mermaid-mcp; use Draw.io for editable architecture diagrams.

### [repo] Repository / Monorepo Strategy

**Functions:** get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development") Â· get_docs_combined(task) Â· get_related_docs("deepseek-api-docs") Â· get_related_docs("flask-docs")

Organize the repository under opencode with separate modules for it-asset-management, service-desk, and defi-integration. Store ADRs in docs/adr, C4 diagrams in docs/c4, and architecture documents in docs/architecture. Use fw-feature-delivery to drive changes and fw-code-review for pull request checks.

### [domains] Functional Domain Decomposition

**Functions:** get_features_combined(task) Â· get_features_combined("it-asset-management") Â· get_features_combined("service-desk") Â· get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Model it-asset-management as a bounded context covering asset lifecycle, inventory, and compliance. Model service-desk as a bounded context for incident, request, and problem management. Keep defi-integration isolated behind an anti-corruption layer and use domain events to communicate between contexts.

### [data] Data Architecture & Schema

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review") Â· get_docs_combined(task) Â· get_related_docs("deepseek-api-docs") Â· get_related_docs("flask-docs")

Define per-service data ownership and use database-per-service for microservices. Use event sourcing or outbox patterns for cross-domain consistency between it-asset-management and service-desk. Validate infrastructure data models with Terraform and consult flask-docs for persistence choices.

### [api] API Methodology

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Define REST or gRPC interfaces for each bounded context, with OpenAPI specs for it-asset-management and service-desk. Use integration patterns like asynchronous events for notifications and sagas for multi-service transactions. Validate API contracts against deepseek-api-docs and flask-docs.

### [frontend] Frontend / UI Methodology

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

If a frontend is needed, use Draw.io or Mermaid Live to render C4 diagrams and architecture views. Keep frontend modules aligned to bounded contexts and consume APIs from it-asset-management and service-desk. Use ng-diagram-mcp-server for interactive diagram previews.

### [agent_model] Agent-MCP Operating Model

**Functions:** get_agents_combined(task) Â· get_agent("opencode") Â· get_agent("crewai") Â· get_mcpservers_combined(task) Â· get_mcp_server("narasimhaponnada-mermaid-mcp") Â· get_mcp_server("erajasekar-ai-diagram-maker-mcp")

Configure opencode as the primary agent with software-architecture and architect-review skills loaded. Use crewai or openhands for parallel review or diagram-generation tasks if needed. Follow fw-feature-delivery for task sequencing and fw-model-development for any ML/LLM components.

### [system_mcp] System MCP Discovery (agent-mcp-orchestrator)

**Functions:** get_mcp_status() Â· list_mcp_servers(filter="") Â· get_server_info(name="<server-name>") Â· get_server_logs(server="<server-name>", limit=20) Â· get_open_incidents()

Use the locally-running agent-mcp-orchestrator MCP to call list_mcp_servers and discover every MCP server available on the system. Call get_mcp_status and get_server_info to verify health and capabilities, then wire suitable servers into the workflow. Use get_server_logs to debug diagram-generation failures and get_open_incidents to avoid degraded servers. Connect narasimhaponnada-mermaid-mcp for C4/ADR diagrams, erajasekar-ai-diagram-maker-mcp and ng-diagram-mcp-server as fallbacks, and any docs/code MCP servers for context.

### [knowledge] Knowledge / Memory Model

**Functions:** get_tools_combined(task) Â· get_tools_combined("Terraform") Â· get_tools_combined("Mermaid Live") Â· get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Load software-architecture and architect-review skills at startup. Consult deepseek-api-docs and flask-docs for implementation constraints. Use ontoly-software-graph to map dependencies and validate architecture relationships.

### [project_knowledge] Project Knowledge Persistence & Re-Advice Loop

**Functions:** get_skill(name) -> save to .mcp-skills/skills/<name>/SKILL.md Â· get_workflow(name) -> save to .mcp-skills/workflows/<name>.md Â· get_mcp_server(name) -> save to .mcp-skills/mcpservers/<name>.json Â· get_agent(name) -> save to .mcp-skills/agents/<name>.json Â· save advice.report + categories + how_to_read -> .mcp-skills/advice/<date>-<slug>.json Â· write/update AGENTS.md (project root) with the manifest + plan Â· set up Obsidian vault (.obsidian + linked notes) + Graphify code graph (graphify-out/graph.json) Â· after each step: update both graphs, query the graph (graphify query/path/explain) Â· pass ONLY the graph summary + this advice as context to the LLM (saves tokens) Â· after each step: get_task_advice(updated status) -> apply refreshed advice

Maintain a project knowledge base with ADRs, C4 diagrams, and architecture decision records. Record feature requirements for it-asset-management and service-desk. Use fw-feature-delivery to update knowledge after each phase.

### [lifecycle] Full Development Lifecycle

**Functions:** get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development")

Follow fw-feature-delivery phases: initialize, design, integrate, verify. In each phase, update ADRs and C4 diagrams before implementation. Use fw-code-review and architect-review as quality gates.

### [testing] Testing Strategy

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review") Â· get_tools_combined(task) Â· get_tools_combined("Terraform") Â· get_tools_combined("Mermaid Live")

Validate Mermaid diagram syntax with Mermaid Live and narasimhaponnada-mermaid-mcp. Run architect-review on all ADRs and architecture documents. Test Terraform plans with terraform validate and terraform plan before applying.

### [deployment] Deployment & Environments

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review") Â· get_mcpservers_combined(task) Â· get_mcp_server("narasimhaponnada-mermaid-mcp") Â· get_mcp_server("erajasekar-ai-diagram-maker-mcp")

Use Terraform to provision infrastructure for it-asset-management and service-desk services. Deploy opencode agents and MCP servers in the same environment. Verify MCP server status via agent-mcp-orchestrator before and after deployment.

### [phases] Implementation Phases

**Functions:** get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development")

Initialize: load skills, set workflow, connect MCP servers. Design: apply DDD, produce ADRs and C4 diagrams, review with architect-review. Integrate & Verify: consult docs, validate with Terraform and Mermaid Live, include it-asset-management and service-desk features.

### [backlog] Master Backlog / TODOs

**Functions:** get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development")

Create backlog items for each bounded context: it-asset-management, service-desk, defi-integration. Add tasks for ADR creation, C4 diagram generation, Terraform validation, and architect-review. Prioritize MCP server wiring and diagram syntax compatibility.

### [done] Definition of Done

**Functions:** get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development") Â· get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Definition of done: ADRs and C4 diagrams exist and pass architect-review. Mermaid syntax validated with Mermaid Live and narasimhaponnada-mermaid-mcp. Terraform plan succeeds and it-asset-management and service-desk features are included.

### [risks] Risks & Mitigations

**Functions:** get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Risk: MCP server diagram syntax incompatibility; mitigate with Mermaid Live validation and fallback servers. Risk: Terraform drift; mitigate by validating plans before apply. Risk: DDD boundaries misaligned with features; mitigate by reviewing with architect-review and ontoly-software-graph.

### [checklist] First Execution Checklist

**Functions:** get_workflows_combined(task) Â· get_workflow("fw-feature-delivery") Â· get_workflow("fw-model-development") Â· get_skills_combined(task) Â· get_skill("software-architecture") Â· get_skill("architect-review")

Load software-architecture and architect-review skills. Connect narasimhaponnada-mermaid-mcp. Define DDD module boundaries. Produce ADRs and C4 diagrams. Run architect-review. Consult deepseek-api-docs and flask-docs. Validate with Terraform and Mermaid Live. Include it-asset-management and service-desk. Verify MCP status via agent-mcp-orchestrator.


