# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Technical Analyst AI agent for a trading/investment system: analyzes price action, chart patterns, candlesticks and indicators (trend, momentum, volatility, volume) across timeframes to produce a structured technical report with levels, market regime and an explicit trading bias |
| Requirement | Technical analyst agent: chart analysis, indicators RSI/MACD/MA/Bollinger, support/resistance, price action, trend analysis, TA signals |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Technical
Analyst agent, following the operating loop in `agent.md` and the
`fw-research-optimize-execute` workflow.

## Recommended (top hits per catalog)

**mcpserver**: kukapay-crypto-indicators-mcp, cryptoanalysismcp, tradingview-mcp-server, binance-cryptocurrency-mcp, alpaca, yahoo-finance, alpha-vantage, financekit-mcp

**skills**: quant-analyst, longbridge-market-data, longbridge, trading-ledger, price-psychology-strategist, ml-data-pipeline, data-storytelling, sql-pro, competitive-landscape

**tools**: pandas-ta, TA-Lib, Backtrader, Pandas, Matplotlib

**workflow**: fw-research-optimize-execute

## Plan

### Phase: Setup
- Load the top skills: quant-analyst, longbridge-market-data, longbridge
- Follow the workflow: fw-research-optimize-execute

### Phase: Implement
- Connect / install the MCP servers: binance-cryptocurrency-mcp, kukapay-crypto-indicators-mcp, financekit-mcp, tradingview-mcp-server
- Use the tools: pandas-ta, TA-Lib, Backtrader

### Phase: Verify
- Verify every indicator value against pulled OHLCV data
- Consult the documentation: tradingview-docs, pandas-ta-docs
- Implement, then verify before completing.

## Ordered Steps

1. Scope: symbol, exchange, timeframes (default M15/H1/D1 + W1 context).
2. Pull + validate OHLCV (`data-loader` subagent).
3. Trend structure + MAs (`trend-scanner`).
4. Indicators: momentum/volatility/volume (`indicator-reader`, quant-analyst).
5. Levels: S/R + entry/exit/invalidation (`levels-engine`).
6. Write report + bias (`bias-writer`), verify before completing.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Every indicator value must carry timeframe + source — no fabricated numbers.
- Never read a chart you have not seen data for.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("quant-analyst")`, `get_skill("longbridge-market-data")`,
  `get_skill("longbridge")`, `get_skill("trading-ledger")`

### mcpservers
- `get_mcp_server("binance-cryptocurrency-mcp")`,
  `get_mcp_server("kukapay-crypto-indicators-mcp")`,
  `get_mcp_server("cryptoanalysismcp")`, `get_mcp_server("tradingview-mcp-server")`

### workflows
- `get_workflow("fw-research-optimize-execute")`

### tools / docs
- `https://github.com/twopirllc/pandas-ta`, `https://ta-lib.org`,
  `https://www.backtrader.com`, `https://www.tradingview.com/support`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : medium (quant-analyst + longbridge cover TA; no dedicated TA skill in catalog)
- mcpservers : strong (crypto-indicators, cryptoanalysis, tradingview, financekit)
- tools : medium (pandas-ta, TA-Lib, Backtrader, Matplotlib)
- workflows : medium (research workflow reused)
- docs / features : medium — web-verify URLs before relying on them

## Research note

The catalog has no dedicated technical-analysis skill; coverage is provided
by `quant-analyst` + `longbridge-market-data`/`longbridge` (125+ TA skills) +
TA indicator MCP servers. For full coverage, install `tradingview-mcp-server`
(30+ TA tools) and `kukapay-crypto-indicators-mcp`.