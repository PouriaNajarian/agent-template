# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Scrum Master Agent for a trading/investment system: facilitates scrum ceremonies (sprint planning, daily standup, sprint review, retrospective), coaches the team (human + AI agents), removes impediments, guards the process, and tracks agile metrics (velocity, burndown, throughput, cycle time, team health) |
| Requirement | Scrum master agent: scrum ceremonies, sprint planning, daily standup, sprint review, retrospective, agile coaching, velocity, burndown, impediment removal |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Scrum
Master agent, following the operating loop in `agent.md` and the scrum
cadence from `fw-role-separated-agents`.

## Recommended (top hits per catalog)

**mcpserver**: jira-sprint-dashboard, jira-mcp, agile-team-mcp-server, agile-luminary, agile-planner-mcp-server, qretro, spranab-saga-mcp, pm33-mcp-server, tacticlaunch-mcp-linear, corbym-backlog-mcp

**skills**: ecom-project-management, team-collaboration-standup-notes, planning-with-files, plan-writing, eng-planning-and-task-breakdown, wrike-automation, dev-brainstorming, context-driven-development, dev-verification-before-completion, dev-dispatching-parallel-agents, eng-spec-driven-development, to-tickets, to-spec, retro, triage

**tools**: Jira, Linear, QRetro, EasyRetro

**workflow**: fw-role-separated-agents, fw-agentic-coding-loop, fw-spec-driven-development

## Plan

### Phase: Setup
- Load the top skills: ecom-project-management, team-collaboration-standup-notes, retro
- Follow the scrum cadence workflow

### Phase: Implement
- Connect / install the MCP servers: jira-mcp, jira-sprint-dashboard, pm33-mcp-server, qretro
- Run the ceremonies: planning, standup, review, retro

### Phase: Verify
- Verify metrics come from the tracker, not vibes
- Consult the documentation: scrum-guide-docs, jira-scrum-docs
- Implement, then verify before completing.

## Ordered Steps

1. Backlog grooming (`backlog-groomer`, `pm33` WSJF).
2. Sprint planning: goal, plan, capacity, owners (`sprint-planner`).
3. Daily standup digest + blockers (`standup-runner`).
4. Guard the sprint: burndown vs ideal (`jira-sprint-dashboard`).
5. Sprint review: demo with evidence.
6. Retrospective + experiments (`retro-facilitator`, `qretro`).
7. Metrics & health report (`metrics-reporter`); verify before completing.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Never claim a sprint is on track without the tracker data.
- Velocity is a trend, not a single sprint.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("ecom-project-management")`,
  `get_skill("team-collaboration-standup-notes")`, `get_skill("retro")`,
  `get_skill("eng-planning-and-task-breakdown")`

### mcpservers
- `get_mcp_server("jira-mcp")`, `get_mcp_server("jira-sprint-dashboard")`,
  `get_mcp_server("pm33-mcp-server")`, `get_mcp_server("qretro")`

### workflows
- `get_workflow("fw-role-separated-agents")`,
  `get_workflow("fw-agentic-coding-loop")`

### tools / docs
- `https://www.atlassian.com/software/jira`, `https://linear.app`,
  `https://qretro.com`, `https://scrumguides.org`,
  `https://support.atlassian.com/jira-software-cloud`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (ecom-project-management, standup notes, retro, planning)
- mcpservers : strong (jira, sprint dashboard, pm33, qretro)
- tools : medium (Jira, Linear, QRetro)
- workflows : medium (role-separated, agentic loop)
- docs / features : medium — web-verify URLs before relying on them