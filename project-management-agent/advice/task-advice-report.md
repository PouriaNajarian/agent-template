# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Project Management Agent for a trading/investment system: plans and tracks projects end-to-end: scope, milestones, task breakdown with dependencies, resource allocation, risk management, scheduling and status reporting; coordinates the other agents as one delivery program |
| Requirement | Project management agent: plan projects, task breakdown, milestone tracking, backlog, risk management, resource allocation, dependencies, status reporting |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Project
Management agent, following the operating loop in `agent.md` and the
`fw-role-separated-agents` workflow.

## Recommended (top hits per catalog)

**mcpserver**: pm33-mcp-server, notion-api-mcp, christulino-todoist-v1-mcp-server, roychri-mcp-server-asana, tacticlaunch-mcp-linear, spranab-saga-mcp, backlog-mcp-server, louis030195-toggl-mcp, corbym-backlog-mcp, jira-mcp, linear-mcp

**skills**: ecom-project-management, plan-writing, eng-planning-and-task-breakdown, planning-with-files, wrike-automation, eng-incremental-implementation, dev-writing-plans, dev-executing-plans, dev-verification-before-completion, to-tickets, wayfinder, triage, to-spec, changeset, eng-spec-driven-development, dev-dispatching-parallel-agents, dev-using-git-worktrees, cron-scheduler, eng-observability-and-instrumentation, context-driven-development

**tools**: Jira, Linear, Notion, Asana, Todoist

**workflow**: fw-role-separated-agents, fw-agentic-coding-loop, fw-spec-driven-development

## Plan

### Phase: Setup
- Load the top skills: eng-planning-and-task-breakdown, plan-writing, to-tickets
- Follow the workflows: fw-role-separated-agents + fw-agentic-coding-loop

### Phase: Implement
- Connect / install the MCP servers: spranab-saga-mcp, jira-mcp, linear-mcp, pm33-mcp-server
- Publish tickets with blocking edges (to-tickets, triage)

### Phase: Verify
- Verify milestones with evidence (dev-verification-before-completion)
- Consult the documentation: jira-docs, linear-docs
- Implement, then verify before completing.

## Ordered Steps

1. Scope & spec (`to-spec`, `eng-spec-driven-development`).
2. Breakdown: phases, milestones, task graph with dependencies (`planner`).
3. Publish tickets with blocking edges (`ticket-writer`, `to-tickets`, `triage`).
4. Sequence: critical path, parallel streams, capacity (`scheduler`).
5. Maintain the risk register (`risk-tracker`).
6. Track daily: rollup from trackers + git + agent reports.
7. Verify milestones with evidence; write status report (`status-reporter`).

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Never report a milestone complete without evidence (tests, gates, accepted deliverables).
- Do not confuse activity with progress; surface blockers, don't hide them.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("eng-planning-and-task-breakdown")`, `get_skill("plan-writing")`,
  `get_skill("to-tickets")`, `get_skill("wayfinder")`, `get_skill("changeset")`

### mcpservers
- `get_mcp_server("pm33-mcp-server")`, `get_mcp_server("spranab-saga-mcp")`,
  `get_mcp_server("jira-mcp")`, `get_mcp_server("linear-mcp")`

### workflows
- `get_workflow("fw-role-separated-agents")`,
  `get_workflow("fw-agentic-coding-loop")`,
  `get_workflow("fw-spec-driven-development")`

### tools / docs
- `https://www.atlassian.com/software/jira`, `https://linear.app`,
  `https://www.notion.so`, `https://support.atlassian.com/jira-software-cloud`,
  `https://linear.app/docs`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (ecom-project-management, plan-writing, to-tickets, wayfinder, changeset)
- mcpservers : strong (pm33, saga tracker, jira, linear, backlog)
- tools : medium (Jira, Linear, Notion, Asana)
- workflows : medium (role-separated, agentic loop, spec-driven)
- docs / features : medium — web-verify URLs before relying on them