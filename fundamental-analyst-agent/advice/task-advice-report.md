# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Fundamental Analyst AI agent for a trading/investment system: analyzes financial statements (income statement, balance sheet, cash flow), SEC filings, earnings quality, growth and valuation (DCF, multiples) to produce a fair-value opinion and investment thesis |
| Requirement | Fundamental analyst agent: financial statement analysis, valuation, DCF, ratios, SEC filings, earnings quality |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Fundamental
Analyst agent, following the operating loop in `agent.md` and the
`fw-research-optimize-execute` workflow.

## Recommended (top hits per catalog)

**mcpserver**: slacking-biz, revelata-deepkpi, eodhd-mcp-server, equibles, secedgar-mcp-server, michalperni11-gif-secfinapi-mcp, unquant, findata-mcp

**skills**: longbridge-fundamentals, quant-analyst, startup-financial-modeling, xvary-stock-research, longbridge-market-data, longbridge, fsi-compliance-checker, competitive-landscape, ml-data-pipeline, sql-pro, market-sizing-analysis, data-storytelling

**tools**: NumPy, Pandas, DuckDB, OpenBB

**workflow**: fw-research-optimize-execute

## Plan

### Phase: Setup
- Load the top skills: longbridge-fundamentals, quant-analyst, xvary-stock-research
- Follow the workflow: fw-research-optimize-execute

### Phase: Implement
- Connect / install the MCP servers: secedgar-mcp-server, equibles, michalperni11-gif-secfinapi-mcp, eodhd-mcp-server, findata-mcp
- Use the tools: Pandas, NumPy, DuckDB, OpenBB

### Phase: Verify
- Verify every figure against filings (never analyst blogs)
- Consult the documentation: sec-edgar-docs, eodhd-docs
- Implement, then verify before completing.

## Ordered Steps

1. Scope: ticker, exchange, fiscal calendar, currency.
2. Gather statements (3+ years) via SEC EDGAR MCPs
   (`statement-reader` subagent).
3. Analyze trends + earnings quality (`quant-analyst`,
   `fsi-compliance-checker`, `quality-auditor` subagent).
4. Business & moat (`competitive-landscape`, `market-sizing-analysis`).
5. Valuation: DCF + multiples with sensitivity (`longbridge-fundamentals`,
   `valuation-engine`, `peer-comparer` subagents).
6. Write thesis + stance; verify before completing.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Every financial figure must carry period + source — no fabricated filings.
- DCF without stated assumptions is not a valuation.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("longbridge-fundamentals")`, `get_skill("quant-analyst")`,
  `get_skill("xvary-stock-research")`, `get_skill("fsi-compliance-checker")`

### mcpservers
- `get_mcp_server("secedgar-mcp-server")`, `get_mcp_server("equibles")`,
  `get_mcp_server("slacking-biz")`, `get_mcp_server("michalperni11-gif-secfinapi-mcp")`,
  `get_mcp_server("eodhd-mcp-server")`

### workflows
- `get_workflow("fw-research-optimize-execute")`

### tools / docs
- `https://numpy.org`, `https://pandas.pydata.org`, `https://duckdb.org`,
  `https://openbb.co`, `https://www.sec.gov/edgar/search`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (longbridge fundamentals + quant + financial modeling)
- mcpservers : strong (finance-fintech SEC EDGAR ecosystem)
- tools : medium (NumPy/Pandas/DuckDB/OpenBB)
- workflows : medium (research workflow reused)
- docs / features : medium — web-verify URLs before relying on them