# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a QA Agent for a trading/investment system: owns test strategy and quality: unit, integration, E2E and regression suites; coverage gates; test-quality audits (anti-patterns, smells); acceptance verification; and the verification-before-completion gate for every other agent's work |
| Requirement | QA agent: test strategy, unit integration e2e tests, test coverage, test automation, playwright browser testing, TDD, regression testing, test anti-patterns, acceptance criteria, verification |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the QA agent,
following the operating loop in `agent.md` and the `test-strategy-rollout`
workflow.

## Recommended (top hits per catalog)

**mcpserver**: playwright (installed :8791), playwright-mcp, cocaxcode-api-testing-mcp, mcp-playwright-server, executeautomation-playwright-mcp-server

**skills**: web-testing-strategy, testing-qa, cf-testing, python-testing-patterns, test-analysis-extensions, e2e-testing, test-automator, javascript-testing-patterns, golang-testing, testing-patterns, claude-webapp-testing, ai-regression-testing, acceptance-orchestrator, k6-load-testing, webapp-testing, unit-testing-test-generate, eng-test-driven-development, dev-test-driven-development, dev-verification-before-completion, test-anti-patterns, test-smell-detection, coverage-analysis, tdd, verify-and-stop, build-scenario-tests

**tools**: Playwright, pytest, Vitest, Jest, K6, Cypress

**workflow**: test-strategy-rollout, tdd-feature-delivery, fw-verification-loop

## Plan

### Phase: Setup
- Load the top skills: web-testing-strategy, testing-qa, e2e-testing
- Follow the workflow: test-strategy-rollout

### Phase: Implement
- Connect / install the MCP servers: playwright (installed), cocaxcode-api-testing-mcp
- Use the tools: Playwright, pytest, Vitest, Jest

### Phase: Verify
- Run the full suite + coverage; confirm output before sign-off
- Consult the documentation: playwright-docs, pytest-docs
- Implement, then verify before completing.

## Ordered Steps

1. Strategy: pyramid + risk matrix (`strategy-planner`, `web-testing-strategy`).
2. Unit/integration tests (`test-writer`, `dev-test-driven-development` + language patterns).
3. E2E critical journeys (`e2e-runner`, `playwright` MCP).
4. Coverage report vs gates (`coverage-analysis`).
5. Quality audit (`quality-auditor`: `test-anti-patterns`, `test-smell-detection`).
6. Regression protection (`ai-regression-testing`, `build-scenario-tests`).
7. Verification gate (`verification-gate`, `dev-verification-before-completion`); sign off with evidence.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Never claim tests pass without running them — evidence over assertions.
- Fix the code, not the test.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("web-testing-strategy")`, `get_skill("testing-qa")`,
  `get_skill("e2e-testing")`, `get_skill("test-anti-patterns")`,
  `get_skill("dev-verification-before-completion")`

### mcpservers
- `get_mcp_server("playwright")`, `get_mcp_server("cocaxcode-api-testing-mcp")`,
  `get_mcp_server("playwright-mcp")`

### workflows
- `get_workflow("test-strategy-rollout")`, `get_workflow("tdd-feature-delivery")`,
  `get_workflow("fw-verification-loop")`

### tools / docs
- `https://playwright.dev`, `https://pytest.org`, `https://vitest.dev`,
  `https://jestjs.io`, `https://playwright.dev/docs`, `https://docs.pytest.org`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (web-testing-strategy, testing-qa, e2e-testing, test quality suite)
- mcpservers : strong (playwright installed, api-testing-mcp)
- tools : strong (Playwright, pytest, Vitest, Jest, k6)
- workflows : strong (test-strategy-rollout, tdd-feature-delivery, fw-verification-loop)
- docs / features : medium — web-verify URLs before relying on them