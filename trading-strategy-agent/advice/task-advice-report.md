# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Trading Strategy Agent for a trading/investment system: designs and specifies trading strategies: signal logic (trend, momentum, mean-reversion, breakout), entry/exit rules, position sizing, risk limits and portfolio allocation — with an explicit, testable rule set ready for the backtest agent |
| Requirement | Trading strategy agent: design strategies, entry/exit rules, risk management, position sizing, portfolio allocation, signal generation |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Trading
Strategy agent, following the operating loop in `agent.md` and the
`fw-model-development` workflow.

## Recommended (top hits per catalog)

**mcpserver**: crashtestyourstrategy, kyurish-trading212-mcp-server, etbars-vibetrader-mcp, arrow-algo, laukikk-alpaca-mcp, alpaca, botspot, keel-trade-keel-trade, alforge-labs-alpha-forge-mcp, alphaassay

**skills**: quant-analyst, trading-ledger, risk-manager, price-psychology-strategist, ml-data-pipeline, ml-model-development, ml-model-evaluation, data-storytelling, sql-pro, longbridge-market-data, longbridge

**tools**: Backtrader, VectorBT, QuantConnect LEAN, Pandas, pandas-ta

**workflow**: fw-model-development, fw-model-evaluation

## Plan

### Phase: Setup
- Load the top skills: quant-analyst, risk-manager, price-psychology-strategist
- Follow the workflows: fw-model-development + fw-model-evaluation

### Phase: Implement
- Connect / install the MCP servers: alpaca, crashtestyourstrategy, alphaassay, alforge-labs-alpha-forge-mcp
- Use the tools: Backtrader, VectorBT, QuantConnect LEAN

### Phase: Verify
- Verify the spec is unambiguous and falsifiable
- Consult the documentation: quantconnect-docs, backtrader-docs
- Implement, then verify before completing.

## Ordered Steps

1. Edge hypothesis (`edge-hunter`): one falsifiable sentence.
2. Universe & timeframe: assets, lookbacks, data requirements.
3. Signal rules (`signal-designer`): exact conditions.
4. Entry/exit/invalidation + execution rules.
5. Position sizing & risk (`risk-designer`): R-multiples, caps, drawdown guard.
6. Test plan (`test-plan-writer`): metrics, IS/OOS, benchmarks, failure criteria.
7. Write the strategy specification (`spec-writer`); verify before completing.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Every rule must be unambiguous — the backtest agent must not need to interpret.
- Risk limits are mandatory, not optional decoration.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("quant-analyst")`, `get_skill("risk-manager")`,
  `get_skill("trading-ledger")`, `get_skill("ml-model-evaluation")`

### mcpservers
- `get_mcp_server("alpaca")`, `get_mcp_server("crashtestyourstrategy")`,
  `get_mcp_server("alphaassay")`, `get_mcp_server("alforge-labs-alpha-forge-mcp")`

### workflows
- `get_workflow("fw-model-development")`, `get_workflow("fw-model-evaluation")`

### tools / docs
- `https://www.backtrader.com`, `https://vectorbt.dev`,
  `https://www.quantconnect.com`, `https://www.quantconnect.com/docs`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (quant-analyst, risk-manager, trading-ledger, ml-model-evaluation)
- mcpservers : strong (strategy/trading ecosystem: alpaca, botspot, arrow-algo, alphaassay, alpha-forge)
- tools : medium (Backtrader, VectorBT, QuantConnect LEAN)
- workflows : medium (model development/evaluation reused)
- docs / features : medium — web-verify URLs before relying on them