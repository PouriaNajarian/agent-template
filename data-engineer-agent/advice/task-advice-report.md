# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Data Engineer AI agent for a trading/investment system: builds data pipelines for market data (ingestion, cleaning, normalization, validation, storage, serving), schema design, migrations, data quality checks, idempotent scheduled ingestion, reproducible dataset versioning |
| Requirement | Data engineer agent: ETL pipelines, market data ingestion, cleaning, normalization, schema design, SQL, data quality, time-series stores |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Data
Engineer agent, following the operating loop in `agent.md` and the
`data-pipeline-build` workflow.

## Recommended (top hits per catalog)

**mcpserver**: data-studio-agent, postgres-mcp (installed), clickhouse, mongodb-mcp-server, dbt-labs-dbt-mcp, airflow-mcp, sixta-connect

**skills**: ml-data-pipeline, data-engineer, database, database-design, database-migration, sql-pro, data-quality-frameworks, data-engineering-data-pipeline, database-architect, database-admin, postgresql-optimization, database-migrations-sql-migrations, sql-schema-design, ml-feature-engineering, cron-scheduler, content-hash-cache-pattern, eng-observability-and-instrumentation, eng-performance-optimization

**tools**: Airflow, dbt, Pandas, DuckDB, Great Expectations

**workflow**: data-pipeline-build, data-warehouse-build, data-pipeline, fw-ml-data-pipeline

## Plan

### Phase: Setup
- Load the top skills: ml-data-pipeline, data-engineer, sql-schema-design
- Follow the workflow: data-pipeline-build

### Phase: Implement
- Connect / install the MCP servers: postgres-mcp, data-studio-agent, clickhouse, dbt-labs-dbt-mcp, airflow-mcp
- Use the tools: Airflow, dbt, Pandas, DuckDB

### Phase: Verify
- Run the quality gates: schema, idempotency, freshness, counts, validation
- Consult the documentation: postgresql-docs, dbt-docs, duckdb-docs
- Implement, then verify before completing.

## Ordered Steps

1. Design the pipeline: sources -> raw -> clean -> curated layers
   (`ml-data-pipeline`, `sql-schema-design`).
2. Design the schema with keys/indexes/partitioning (`database-design`,
   `database-architect`, `sql-pro`, `postgresql-optimization`).
3. Implement ingestion + transforms (`data-engineer`,
   `data-engineering-data-pipeline`, `ml-feature-engineering`,
   `content-hash-cache-pattern`).
4. Wire data quality (`data-quality-frameworks`, `eng-observability-and-instrumentation`).
5. Schedule idempotent runs (`cron-scheduler`).
6. Run end-to-end, verify gates, write the data dictionary.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Never ship a pipeline that has not been run end-to-end.
- Time-series data must be exchange-session aware (no weekend/holiday gaps).

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("ml-data-pipeline")`, `get_skill("data-engineer")`,
  `get_skill("data-quality-frameworks")`, `get_skill("database-design")`,
  `get_skill("sql-pro")`, `get_skill("cron-scheduler")`

### mcpservers
- `get_mcp_server("data-studio-agent")`, `get_mcp_server("postgres-mcp")`,
  `get_mcp_server("dbt-labs-dbt-mcp")`, `get_mcp_server("airflow-mcp")`

### workflows
- `get_workflow("data-pipeline-build")`, `get_workflow("data-warehouse-build")`,
  `get_workflow("fw-ml-data-pipeline")`

### tools / docs
- `https://airflow.apache.org`, `https://www.getdbt.com`,
  `https://pandas.pydata.org`, `https://duckdb.org`,
  `https://www.postgresql.org/docs`, `https://docs.getdbt.com`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (data/database catalog coverage excellent)
- mcpservers : strong (database catalog 958 entries)
- workflows : strong (data-pipeline-build, data-warehouse-build, fw-ml-data-pipeline)
- tools : strong (Airflow, dbt, Pandas, DuckDB)
- docs / features : medium — web-verify URLs before relying on them