# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build an MCP/Tool Agent for a trading/investment system: owns the tool layer: discovers, selects, builds, configures, tests and maintains MCP servers and tool integrations; queries the orchestrator for live inventory, builds new MCP servers when a capability is missing, keeps every agent's tool contract documented and tested |
| Requirement | MCP/tool agent: build MCP servers, integrate tools and APIs, tool selection, MCP server development, API connectors, orchestration, tool inventory |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the MCP/Tool
agent, following the operating loop in `agent.md` and the `api-integration`
workflow.

## Recommended (top hits per catalog)

**mcpserver**: mikkoparkkola-mcp-gateway, mcp360-universal-gateway, markgatcha-universal-mcp-toolkit, ai-security-gateway-mcp-gateway

**skills**: mcp-tool-developer, claude-mcp-builder, mcp-builder-ms, mcp-apps-builder, ms-azure-aigateway, n8n-mcp-tools-expert, global-chat-agent-discovery, not-human-search-mcp, agent-harness-fault-injection, api-design, api-connector-builder, api-contract-testing, create-custom-agent, eng-api-and-interface-design, eng-test-driven-development, eng-observability-and-instrumentation

**tools**: FastMCP, MCP SDK, mcp-gateway, pytest, OmniRoute

**workflow**: api-integration, fw-ai-agent-setup

## Plan

### Phase: Setup
- Load the top skills: mcp-tool-developer, claude-mcp-builder, mcp-apps-builder
- Follow the workflow: api-integration

### Phase: Implement
- Discover via orchestrator + mcp-skills + registries (global-chat-agent-discovery)
- Install/configure via orchestrator install_mcp_server; build custom with mcp-tool-developer

### Phase: Verify
- Verify: get_server_functions + live smoke test; check logs
- Document the contract for the consuming agent
- Implement, then verify before completing.

## Ordered Steps

1. Requirement: capability, consumer agent, security constraints.
2. Discover: orchestrator catalog + mcp-skills + registries (`tool-scout`).
3. Evaluate shortlist: fit, platform, security, maintenance, license.
4. Install/configure via orchestrator, or build custom (`mcp-builder`).
5. Verify: functions + smoke test + logs (`verifier`).
6. Document contract per consuming agent (`contract-writer`).
7. Test failure modes; update agent configs via orchestrator sync; verify before completing.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Never install a server without testing it; never hardcode keys.
- Always sync agent configs through the orchestrator.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("mcp-tool-developer")`, `get_skill("claude-mcp-builder")`,
  `get_skill("mcp-apps-builder")`, `get_skill("api-contract-testing")`

### mcpservers
- `get_mcp_server("mikkoparkkola-mcp-gateway")`,
  `get_mcp_server("mcp360-universal-gateway")`,
  `get_mcp_server("ai-security-gateway-mcp-gateway")`

### workflows
- `get_workflow("api-integration")`, `get_workflow("fw-ai-agent-setup")`

### tools / docs
- `https://github.com/jlowin/fastmcp`,
  `https://github.com/modelcontextprotocol/typescript-sdk`,
  `https://github.com/MikkoParkkola/mcp-gateway`, `https://modelcontextprotocol.io`

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (mcp-tool-developer, claude-mcp-builder, mcp-apps-builder, api-contract-testing)
- mcpservers : strong (gateway ecosystem: mcp-gateway, mcp360, ai-security-gateway)
- tools : medium (FastMCP, MCP SDK, pytest)
- workflows : medium (api-integration reused)
- docs / features : medium — web-verify URLs before relying on them