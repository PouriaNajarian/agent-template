---
name: code-review
description: Review code for correctness, security, readability, and maintainability before merging.
category: engineering
keywords: [review, pr, merge, quality, lint]
when_to_use: Before merging a change or when asked to assess code quality.
---

# Code Review

> **Catalog-only seed skill.** The mcp-skills catalog indexes this skill, but no
> `SKILL.md` source file exists on disk (`path: null` in `skills.json`).
> The metadata above is the authoritative catalog entry; see
> `_catalog-entry.json` for the raw form. For full instructions use the
> companion skills copied alongside this one (`eng-code-review-and-quality`,
> `web-code-review`, `differential-review`, `review-swarm`,
> `code-review-checklist`).

## Intent

Review a change for correctness, security, readability and maintainability
before it merges. Produce findings with severity, concrete fixes, and an
explicit approve / request-changes decision.

## Example task

"Review the checkout PR for security issues."
