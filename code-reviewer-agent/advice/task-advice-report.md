# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (`get_task_advice`).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a code reviewer AI agent: review code diffs and pull requests for correctness, security, performance, style and test coverage; produce structured review reports; use workflows, skills, MCP servers and subagents |
| Requirement | Build a code reviewer AI agent: review code diffs and pull requests for correctness, security, performance, style and test coverage; produce structured review reports; use workflows, skills, MCP servers and subagents |
| Advice elapsed | 306.3 s |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows (all 7 catalogs) |

## Summary

Apply the recommended skills, workflow and tools for: Build a code reviewer AI agent: review code diffs and pull requests for correctness, security, performance, style and test coverage; produce structured review reports; use workflows, skills, MCP servers and subagents

## Recommended (top hits per catalog)

**agent**: langflow

**docs**: crypto-exchange-guide, itil-axelos-docs

**features**: performance-review, reviews-ratings

**mcpserver**: notasandy-mcp-code-sanitizer

**skills**: code-review, vibers-code-review

**tools**: MinIO, Terraform

**workflow**: fw-code-review

## Plan

### Phase: Setup
- Load the top skills: code-review, vibers-code-review
- Follow the workflow: fw-code-review

### Phase: Implement
- Connect / install the MCP server: notasandy-mcp-code-sanitizer
- Use the agent/platform: langflow

### Phase: Verify
- Consult the documentation: crypto-exchange-guide, itil-axelos-docs
- Use the open-source tools: MinIO, Terraform
- Include the features: performance-review, reviews-ratings
- Implement, then verify before completing.

## Ordered Steps

1. Use the open-source tools: MinIO, Terraform
2. Include the features: performance-review, reviews-ratings
3. Implement, then verify before completing.
4. Live research first: catalog coverage is weak (skills, workflows, mcpservers, agents, docs, tools). Run discover_all('Build a code reviewer AI agent: review code diffs and pull requests for correctness, security, perfo') and web-search the official repos, then re-run get_task_advice.
5. PERSIST everything into the working project: save every skill/workflow/MCP/agent/docs you load under .mcp-skills/ (skills/, workflows/, mcpservers/, agents/, docs/) plus this full advice under .mcp-skills/advice/, write/update AGENTS.md at the project root with the manifest + this plan, and set up a knowledge graph in the project: Obsidian vault (.obsidian + linked notes per domain/decision/component) and a Graphify code graph (graphify-out/graph.json built from the codebase).
6. REPORT BACK after each step: update the Obsidian notes + Graphify graph with what changed, run a graph query (graphify query/path/explain) and pass ONLY the relevant graph summary + this advice as context to the LLM (prevents token burning), then call get_task_advice with the updated status, persist the refreshed advice into .mcp-skills/advice/, and continue - never run multiple steps without re-checking.

## Cautions

_(none returned)_

## How to read (exact follow-up MCP calls)

### agents
- `get_agent("langflow")`
- `get_agent("flowise")`
- `get_agent("dify")`

### docs
- `https://www.binance.com/en/research`
- `https://www.axelos.com/certifications/itil-service-management`
- `https://gofastmcp.com`

### features
- `https://www.sap.com/products/erp.html`
- ``
- `https://www.binance.com/en/research`

### mcpservers
- `get_mcp_server("notasandy-mcp-code-sanitizer")`
- `get_mcp_server("corbym-backlog-mcp")`
- `get_mcp_server("xjtlumedia-ai-hr-management-toolkit")`
- `get_mcp_server("astandrik-codex-pets")`
- `get_mcp_server("selvage-lab-selvage")`

### skills
- `get_skill("code-review")`
- `get_skill("vibers-code-review")`
- `get_skill("performance-testing-review-multi-agent-review")`
- `get_skill("openai-security-best-practices")`
- `get_skill("differential-review")`
- `get_skill("cf-code-review")`
- `get_skill("gh-review-requests")`
- `get_skill("git-pr-workflows-git-workflow")`

### tools
- `https://min.io`
- `https://www.terraform.io`
- `https://www.langchain.com`

### workflows
- `get_workflow("fw-code-review")` → workflows/framework/fw-code-review.md
- `get_workflow("code-review-pass")` → workflows/quality/code-review-pass.md
- `get_workflow("fix-slow-checkout-page")` → workflows/general/fix-slow-checkout-page.md
- `get_workflow("fw-feature-delivery")` → workflows/framework/fw-feature-delivery.md
- `get_workflow("fw-role-separated-agents")` → workflows/framework/fw-role-separated-agents.md

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- agents : 0.033
- docs : 0.033
- mcpservers : 0.033
- skills : 0.033
- tools : 0.033
- workflows : 0.033

## Research note

- tool: `discover_all("Build a code reviewer AI agent: review code diffs and pull requests for correctness, security, performance, style and te")`
- weak catalogs: skills, workflows, mcpservers, agents, docs, tools
- note: Catalog coverage is weak for this task (skills, workflows, mcpservers, agents, docs, tools) - the ranked hits may be irrelevant. Run live research first, then re-run get_task_advice.

- search_mcp_servers(query="<key tool>")
- search_agents(query="<key tool>")
- web search the official repos/docs for exact URLs and install commands

## Methodology Report (20 sections)

### [executive_summary] Executive Summary

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review") get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass")`

Apply the recommended skills, workflow and tools for: Build a code reviewer AI agent: review code diffs and pull requests for correctness, security, performance, style and test coverage; produce structured review reports; use workflows, skills, MCP servers and subagents

### [goals] Goals & Success Criteria

**Functions:** `get_features_combined(task) get_features_combined("performance-review") get_features_combined("reviews-ratings") get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass")`

Use the top-ranked features: performance-review, reviews-ratings, crypto-wallets. Use the top-ranked workflows: fw-code-review, code-review-pass, fix-slow-checkout-page.

### [constraints] Core Engineering Constraints

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review") get_docs_combined(task) get_related_docs("crypto-exchange-guide") get_related_docs("itil-axelos-docs")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review. Use the top-ranked docs: crypto-exchange-guide, itil-axelos-docs, fastmcp-docs.

### [architecture] Target Architecture & Stack

**Functions:** `get_mcpservers_combined(task) get_mcp_server("notasandy-mcp-code-sanitizer") get_mcp_server("corbym-backlog-mcp") get_agents_combined(task) get_agent("langflow") get_agent("flowise") get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Use the top-ranked mcpservers: notasandy-mcp-code-sanitizer, corbym-backlog-mcp, xjtlumedia-ai-hr-management-toolkit. Use the top-ranked agents: langflow, flowise, dify. Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review.

### [repo] Repository / Monorepo Strategy

**Functions:** `get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass") get_docs_combined(task) get_related_docs("crypto-exchange-guide") get_related_docs("itil-axelos-docs")`

Use the top-ranked workflows: fw-code-review, code-review-pass, fix-slow-checkout-page. Use the top-ranked docs: crypto-exchange-guide, itil-axelos-docs, fastmcp-docs.

### [domains] Functional Domain Decomposition

**Functions:** `get_features_combined(task) get_features_combined("performance-review") get_features_combined("reviews-ratings") get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Use the top-ranked features: performance-review, reviews-ratings, crypto-wallets. Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review.

### [data] Data Architecture & Schema

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review") get_docs_combined(task) get_related_docs("crypto-exchange-guide") get_related_docs("itil-axelos-docs")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review. Use the top-ranked docs: crypto-exchange-guide, itil-axelos-docs, fastmcp-docs.

### [api] API Methodology

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review.

### [frontend] Frontend / UI Methodology

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review.

### [agent_model] Agent-MCP Operating Model

**Functions:** `get_agents_combined(task) get_agent("langflow") get_agent("flowise") get_mcpservers_combined(task) get_mcp_server("notasandy-mcp-code-sanitizer") get_mcp_server("corbym-backlog-mcp")`

Use the top-ranked agents: langflow, flowise, dify. Use the top-ranked mcpservers: notasandy-mcp-code-sanitizer, corbym-backlog-mcp, xjtlumedia-ai-hr-management-toolkit.

### [system_mcp] System MCP Discovery (agent-mcp-orchestrator)

**Functions:** `get_mcp_status() list_mcp_servers(filter="") get_server_info(name="<server-name>") get_server_logs(server="<server-name>", limit=20) get_open_incidents()`

The agent-mcp-orchestrator MCP is running locally. Call its tools to discover every MCP server on this system and connect the suitable ones where the sections above need them: get_mcp_status() for live status, list_mcp_servers(filter) for all known servers (transport, port, install, agent hints), get_server_info(name) for how to connect one, get_server_logs(server) for health, get_open_incidents() for known problems.

### [knowledge] Knowledge / Memory Model

**Functions:** `get_tools_combined(task) get_tools_combined("MinIO") get_tools_combined("Terraform") get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Use the top-ranked tools: MinIO, Terraform, LangChain. Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review.

### [project_knowledge] Project Knowledge Persistence & Re-Advice Loop

**Functions:** `get_skill(name) -> save to .mcp-skills/skills/<name>/SKILL.md get_workflow(name) -> save to .mcp-skills/workflows/<name>.md get_mcp_server(name) -> save to .mcp-skills/mcpservers/<name>.json get_agent(name) -> save to .mcp-skills/agents/<name>.json save advice.report + categories + how_to_read -> .mcp-skills/advice/<date>-<slug>.json write/update AGENTS.md (project root) with the manifest + plan set up Obsidian vault (.obsidian + linked notes) + Graphify code graph (graphify-out/graph.json) after each step: update both graphs, query the graph (graphify query/path/explain) pass ONLY the graph summary + this advice as context to the LLM (saves tokens) after each step: get_task_advice(updated status) -> apply refreshed advice`

Persist EVERYTHING this advice produced into the working project so later sessions can use it without re-asking: save every skill you load (get_skill) under .mcp-skills/skills/<name>/SKILL.md, workflows under .mcp-skills/workflows/<name>.md, MCP server and agent details under .mcp-skills/mcpservers/ and .mcp-skills/agents/, docs under .mcp-skills/docs/, and this full advice (report + categories + how_to_read) under .mcp-skills/advice/<date>-<slug>.json. Write/update AGENTS.md at the project root with a manifest of every saved item plus the execution plan. Set up and maintain a knowledge graph in EVERY project: (1) an Obsidian vault (.obsidian + notes per domain/decision/component, e.g. docs/obsidian/ with .md notes linked to each other) and (2) a Graphify code graph (graphify-out/graph.json built from the codebase with graphify). After EACH step, update both graphs (new notes/nodes/edges) and before continuing, run a graph query (graphify query/path/explain) and pass ONLY the relevant graph summary + this advice summary back to the LLM as context - this prevents token burning by avoiding re-feeding the full history. After each step also report back to the mcp-skills server (get_task_advice with the updated status) and apply the refreshed advice before continuing.

### [lifecycle] Full Development Lifecycle

**Functions:** `get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass")`

Load the top skills: code-review, vibers-code-review Follow the workflow: fw-code-review Connect / install the MCP server: notasandy-mcp-code-sanitizer Use the agent/platform: langflow Consult the documentation: crypto-exchange-guide, itil-axelos-docs Use the open-source tools: MinIO, Terraform Include the features: performance-review, reviews-ratings Implement, then verify before completing.

### [testing] Testing Strategy

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review") get_tools_combined(task) get_tools_combined("MinIO") get_tools_combined("Terraform")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review. Use the top-ranked tools: MinIO, Terraform, LangChain.

### [deployment] Deployment & Environments

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review") get_mcpservers_combined(task) get_mcp_server("notasandy-mcp-code-sanitizer") get_mcp_server("corbym-backlog-mcp")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review. Use the top-ranked mcpservers: notasandy-mcp-code-sanitizer, corbym-backlog-mcp, xjtlumedia-ai-hr-management-toolkit.

### [phases] Implementation Phases

**Functions:** `get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass")`

Setup: Load the top skills: code-review, vibers-code-review; Follow the workflow: fw-code-review Implement: Connect / install the MCP server: notasandy-mcp-code-sanitizer; Use the agent/platform: langflow Verify: Consult the documentation: crypto-exchange-guide, itil-axelos-docs; Use the open-source tools: MinIO, Terraform; Include the features: performance-review, reviews-ratings; Implement, then verify before completing.

### [backlog] Master Backlog / TODOs

**Functions:** `get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass")`

Load the top skills: code-review, vibers-code-review Follow the workflow: fw-code-review Connect / install the MCP server: notasandy-mcp-code-sanitizer Use the agent/platform: langflow Consult the documentation: crypto-exchange-guide, itil-axelos-docs Use the open-source tools: MinIO, Terraform Include the features: performance-review, reviews-ratings Implement, then verify before completing.

### [done] Definition of Done

**Functions:** `get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass") get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Implement, verify locally (lint/typecheck/tests), deploy a preview, smoke-test the deployed version, then record evidence before declaring done.

### [risks] Risks & Mitigations

**Functions:** `get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Use the top-ranked skills: code-review, vibers-code-review, performance-testing-review-multi-agent-review.

### [checklist] First Execution Checklist

**Functions:** `get_workflows_combined(task) get_workflow("fw-code-review") get_workflow("code-review-pass") get_skills_combined(task) get_skill("code-review") get_skill("vibers-code-review")`

Load the top skills: code-review, vibers-code-review Follow the workflow: fw-code-review Connect / install the MCP server: notasandy-mcp-code-sanitizer Use the agent/platform: langflow Consult the documentation: crypto-exchange-guide, itil-axelos-docs Use the open-source tools: MinIO, Terraform Include the features: performance-review, reviews-ratings Implement, then verify before completing.

## Past advice reused (nearest cached reports)

- similarity=0.3394 · llm=auto/coding:free · `Test a market-analysis webapp running on port 8080 with server-rendered templates, local auth (login/signup), admin panel at /admin, ANN charts page, and crypto payments page using Playwright browser `
- similarity=0.3252 · llm=deepseek-v4-flash · `Configure the OmniRoute frontend at http://localhost:20128/home to integrate web cookie-based providers (e.g., ChatGPT WebChat) using agent-mcp-orchestrator, WinRemore MCP, Playwright, and other autom`
- similarity=0.3095 · llm=deepseek-v4-pro · `optimize and improve leather store Next.js Hono Cloudflare Workers project for performance, security, SEO, accessibility, bundle size`

