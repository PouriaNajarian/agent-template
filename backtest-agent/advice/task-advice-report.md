# mcp-skills — Full Task Advice

> Generated by the local **mcp-skills** MCP server (vector ranking; DeepSeek
> combined ranker timed out at build time).
> Dashboard: http://localhost:8787  ·  MCP: http://localhost:8788/mcp

| Field | Value |
|---|---|
| Task | Build a Backtest Agent for a trading/investment system: implements and runs backtests of strategy specifications: executes rules against historical data with realistic costs, computes performance/risk metrics (expectancy, Sharpe, max drawdown, hit rate, exposure), runs IS/OOS and walk-forward analysis, detects overfitting, and produces a verdict with confidence |
| Requirement | Backtest agent: run backtests, metrics sharpe max drawdown, walk-forward, overfitting detection, statistical significance, performance attribution |
| Ranking engine | vector (HashEmbedder index, 2813 skills / 83 workflows / 13539 mcpservers) |
| Categories returned | agents, docs, features, mcpservers, skills, tools, workflows |

## Summary

Apply the recommended skills, workflow and tools for building the Backtest
agent, following the operating loop in `agent.md` and the
`fw-model-evaluation` workflow.

## Recommended (top hits per catalog)

**mcpserver**: alphaassay, alforge-labs-alpha-forge-mcp, tradingview-backtest-assistant, dolphinquant-echolon, crashtestyourstrategy, keel-trade-keel-trade

**skills**: backtesting-frameworks, quant-analyst, ml-model-evaluation, ml-data-pipeline, risk-manager, trading-ledger, data-storytelling, sql-pro, eng-test-driven-development, dev-verification-before-completion

**tools**: Backtrader, VectorBT, QuantStats, Pandas, SciPy

**workflow**: fw-model-evaluation, fw-model-development

## Plan

### Phase: Setup
- Load the top skills: backtesting-frameworks, quant-analyst, ml-model-evaluation
- Follow the workflow: fw-model-evaluation

### Phase: Implement
- Connect / install the MCP servers: alphaassay, alforge-labs-alpha-forge-mcp, tradingview-backtest-assistant
- Use the tools: Backtrader, VectorBT, QuantStats

### Phase: Verify
- Verify the backtest ran, fees applied, IS/OOS clean, overfitting checked
- Consult the documentation: backtrader-docs, vectorbt-docs
- Implement, then verify before completing.

## Ordered Steps

1. Read the spec; verify unambiguity (`spec-interpreter`); escalate ambiguities.
2. Pull/validate historical data (`ml-data-pipeline`, `postgres-mcp`).
3. Implement the strategy with tests (`backtesting-frameworks`, `eng-test-driven-development`).
4. Run full-period + IS/OOS + walk-forward with fees/slippage.
5. Compute metrics (`metrics-computer`): expectancy, Sharpe, max DD, hit rate, exposure.
6. Validate: overfitting checks (alphaassay deflated Sharpe, crashtestyourstrategy stress).
7. Write report + verdict (`report-writer`); verify before completing.

## Cautions

- DeepSeek combined ranker was unavailable (MCP timeout); vector rankings used.
  Re-run `get_task_advice` when the backend is healthy to refresh advice/.
- Never report a backtest you have not run; never tune on test data.
- Fees/slippage and IS/OOS separation are mandatory.

## How to read (exact follow-up MCP calls)

### skills
- `get_skill("backtesting-frameworks")`, `get_skill("quant-analyst")`,
  `get_skill("ml-model-evaluation")`, `get_skill("risk-manager")`

### mcpservers
- `get_mcp_server("alphaassay")`, `get_mcp_server("alforge-labs-alpha-forge-mcp")`,
  `get_mcp_server("tradingview-backtest-assistant")`, `get_mcp_server("crashtestyourstrategy")`

### workflows
- `get_workflow("fw-model-evaluation")`, `get_workflow("fw-model-development")`

### tools / docs
- `https://www.backtrader.com`, `https://vectorbt.dev`,
  `https://github.com/ranaroussi/quantstats`,
  `https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551` (deflated Sharpe)

## Combined ranking functions (best default per catalog)

- agents : `get_agents_combined(task)`
- docs : `get_docs_combined(task)`
- features : `get_features_combined(task)`
- mcpservers : `get_mcpservers_combined(task)`
- skills : `get_skills_combined(task)`
- tools : `get_tools_combined(task)`
- workflows : `get_workflows_combined(task)`

## Coverage (per catalog hit strength)

- skills : strong (backtesting-frameworks, quant-analyst, ml-model-evaluation)
- mcpservers : strong (alphaassay, alpha-forge, tradingview backtest, crashtestyourstrategy)
- tools : medium (Backtrader, VectorBT, QuantStats, SciPy)
- workflows : medium (model evaluation reused)
- docs / features : medium — web-verify URLs before relying on them